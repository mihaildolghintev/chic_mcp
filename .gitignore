# Binaries
/server
/bin/
/out/
*.exe

# Local env / secrets
.env
*.local
# Raw deploy secrets for laptop deploys (template: .envrc.example).
# NB: .kamal/secrets is committed — it only maps env vars, no raw values.
.envrc

# SQLite cache
*.db
*.db-shm
*.db-wal
/data/

# OS / editor
.DS_Store
